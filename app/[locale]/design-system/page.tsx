import { DesignSystemShowcase } from "@/components/design-system/showcase";

export const metadata = {
  title: "Design System — Nordic Night",
  robots: { index: false },
};

export default function DesignSystemPage() {
  return <DesignSystemShowcase />;
}
